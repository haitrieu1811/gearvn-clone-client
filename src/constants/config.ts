const CONFIG = {
  PORT: import.meta.env.VITE_PORT,
  BACKEND_URL: import.meta.env.VITE_BACKEND_URL,
  TABLET_SCREEN_SIZE: import.meta.env.VITE_TABLET_SCREEN_SIZE,
  MOBILE_SCREEN_SIZE: import.meta.env.VITE_MOBILE_SCREEN_SIZE
};

export default CONFIG;
